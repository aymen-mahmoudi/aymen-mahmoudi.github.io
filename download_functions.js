var dict = {
    link_cv_en: 'https://filedn.eu/lURhr7yoDog5QqszLEQkrtu/Personal_webpage/CVs/MAHMOUDI_EN.pdf',
    link_cv_fr: 'https://filedn.eu/lURhr7yoDog5QqszLEQkrtu/Personal_webpage/CVs/MAHMOUDI_FR.pdf',
    link_rec_AO: 'https://filedn.eu/lURhr7yoDog5QqszLEQkrtu/Personal_webpage/Recommendations/Abdelkarim-Ouerghi.pdf',
    link_rec_VB:'https://filedn.eu/lURhr7yoDog5QqszLEQkrtu/Personal_webpage/Recommendations/Vincent-Bouchiat.pdf',
    link_rec_PT:'https://filedn.eu/lURhr7yoDog5QqszLEQkrtu/Personal_webpage/Recommendations/Pascal-Turban.pdf',
    link_rec_SG:'https://filedn.eu/lURhr7yoDog5QqszLEQkrtu/Personal_webpage/Recommendations/Soraya-Ababou.pdf',
    link_rec_SH:'https://filedn.eu/lURhr7yoDog5QqszLEQkrtu/Personal_webpage/Recommendations/Sonia-Haddad.pdf',
    link_att_polytech_vacation:'https://filedn.eu/lURhr7yoDog5QqszLEQkrtu/Personal_webpage/Attestations/Polytech_1.pdf',
    link_att_polytech_monitorat:'https://filedn.eu/lURhr7yoDog5QqszLEQkrtu/Personal_webpage/Attestations/Polytech_2.pdf',
    link_att_IUT_monitorat:'https://filedn.eu/lURhr7yoDog5QqszLEQkrtu/Personal_webpage/Attestations/Monitorat-IUT.pdf'};
  
  
  
// Function to open a link after password verification
function openLinkWithPassword_cv_en() {
    // Prompt the user for a password
    var password = prompt("Please enter the password:");
    
    // Check if the password is correct
    if (password === "paris75") {
        // Replace 'https://example.com' with the link you want to open
        window.open(dict.link_cv_en, '_blank');
    } else {
        alert("Incorrect password. Please try again.");
    }
}

// Function to open a link after password verification
function openLinkWithPassword_cv_fr() {
    // Prompt the user for a password
    var password = prompt("Please enter the password:");
    
    // Check if the password is correct
    if (password === "paris75") {
        // Replace 'https://example.com' with the link you want to open
        window.open(dict.link_cv_fr, '_blank');
    } else {
        alert("Incorrect password. Please try again.");
    }
}

// Function to open a link after password verification
function openLinkWithPassword_rec_AO() {
    // Prompt the user for a password
    var password = prompt("Please enter the password:");
    
    // Check if the password is correct
    if (password === "paris75") {
        // Replace 'https://example.com' with the link you want to open
        window.open(dict.link_rec_AO, '_blank');
    } else {
        alert("Incorrect password. Please try again.");
    }
}

// Function to open a link after password verification
function openLinkWithPassword_rec_VB() {
    // Prompt the user for a password
    var password = prompt("Please enter the password:");
    
    // Check if the password is correct
    if (password === "paris75") {
        // Replace 'https://example.com' with the link you want to open
        window.open(dict.link_rec_VB, '_blank');
    } else {
        alert("Incorrect password. Please try again.");
    }
}

// Function to open a link after password verification
function openLinkWithPassword_rec_PT() {
    // Prompt the user for a password
    var password = prompt("Please enter the password:");
    
    // Check if the password is correct
    if (password === "paris75") {
        // Replace 'https://example.com' with the link you want to open
        window.open(dict.link_rec_PT, '_blank');
    } else {
        alert("Incorrect password. Please try again.");
    }
}

// Function to open a link after password verification
function openLinkWithPassword_rec_SG() {
    // Prompt the user for a password
    var password = prompt("Please enter the password:");
    
    // Check if the password is correct
    if (password === "paris75") {
        // Replace 'https://example.com' with the link you want to open
        window.open(dict.link_rec_SG, '_blank');
    } else {
        alert("Incorrect password. Please try again.");
    }
}

// Function to open a link after password verification
function openLinkWithPassword_rec_SH() {
    // Prompt the user for a password
    var password = prompt("Please enter the password:");
    
    // Check if the password is correct
    if (password === "paris75") {
        // Replace 'https://example.com' with the link you want to open
        window.open(dict.link_rec_SH, '_blank');
    } else {
        alert("Incorrect password. Please try again.");
    }
}

// Function to open a link after password verification
function openLinkWithPassword_att_Polytech_vacation() {
    // Prompt the user for a password
    var password = prompt("Please enter the password:");
    
    // Check if the password is correct
    if (password === "paris75") {
        // Replace 'https://example.com' with the link you want to open
        window.open(dict.link_att_polytech_vacation, '_blank');
    } else {
        alert("Incorrect password. Please try again.");
    }
}

// Function to open a link after password verification
function openLinkWithPassword_att_Polytech_monitorat() {
    // Prompt the user for a password
    var password = prompt("Please enter the password:");
    
    // Check if the password is correct
    if (password === "paris75") {
        // Replace 'https://example.com' with the link you want to open
        window.open(dict.link_att_polytech_monitorat, '_blank');
    } else {
        alert("Incorrect password. Please try again.");
    }
}

// Function to open a link after password verification
function openLinkWithPassword_att_IUT_monitorat() {
    // Prompt the user for a password
    var password = prompt("Please enter the password:");
    
    // Check if the password is correct
    if (password === "paris75") {
        // Replace 'https://example.com' with the link you want to open
        window.open(dict.link_att_IUT_monitorat, '_blank');
    } else {
        alert("Incorrect password. Please try again.");
    }
}